export interface User {
  id: string
  name: string
  avatar: string
  grade: string
  dorm: string
  preferences: string[]
  joinDate: string
  gameCount: number
  dmCount: number
  circleCount: number
}
